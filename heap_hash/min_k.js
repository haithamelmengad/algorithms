"use strict";

// MIN K

// Min k is a deceptively simple-sounding set of problems involving finding
// the minimum (or maximum) k values from an array.

// Write a function that, given an array and a k, returns the min k elements
// in that array in order from lowest to highest. Try doing this without
// completely sorting the array.
// The runtime complexity of your solution should be O(n log(k))

// Examples:
// min_k([1, 2, 3], 0) -> []
// min_k([1], 1) -> [1]
// min_k([5, 2, 9], 2) -> [2, 5]

// Hint: Use a heap
var min_k = function(array, k) {
  // YOUR CODE HERE
};

window.min_k = min_k;
